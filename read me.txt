"""
web_search_tool.py
------------------
PART 1: Web Search Tool.

Takes a text query and returns a list of web results in a common
dict format that the rest of the pipeline (merge + fact-check) expects.

Uses DuckDuckGo search (no API key required, free, good enough for a
university project). If you ever need something more robust/reliable,
swap the internals of `web_search()` for a SerpAPI call — the function
signature and return format stay the same, so nothing else breaks.
"""
def web_search(query: str, max_results: int = 5, retries: int = 2):
    """
    Search the web for a query and return a clean list of results.

    Args:
        query: the search string (usually generated by the planner
               or derived from the user's original question)
        max_results: how many results to fetch
        retries: how many times to retry if DuckDuckGo rate-limits us

    Returns:
        List[dict] each shaped like:
        {
            "text": str,        # snippet/body text
            "title": str,
            "url": str,
            "source_type": "web"
        }
    """

"""
evidence_merger.py
------------------
PART 2: Evidence Retrieval (Merging).

Takes the raw outputs from Person B's tools (Papers via ChromaDB,
Database via SQLite) and your own web_search_tool.py, and merges
them into ONE unified evidence pool with a consistent shape.

This solves the #1 group-project failure mode: everyone's data
comes back in a different format. Everything downstream (fact
checking, report generation) only ever needs to look at this one
unified shape:
    {
        "id": str,            # unique id for this piece of evidence
        "text": str,          # the actual content/snippet
        "source_type": str,   # "paper" | "database" | "web"
        "source_id": str,     # doc id / row id / url
        "score": float|None,  # similarity/confidence score if available
        "meta": dict          # anything extra (title, author, table name, etc.)
    }
"""

def normalize_paper_result(r: dict) -> dict:
    """
    Adapt Person B's ChromaDB paper search output to the unified shape.
    NOTE: confirm these key names with Person B — this is a best guess
    based on typical ChromaDB + sentence-transformers RAG setups.
    Expected input shape (example):
        {"chunk": "...", "doc_id": "...", "score": 0.83, "title": "..."}
    """
def normalize_db_result(r: dict) -> dict:
    """
    Adapt Person B's SQLite database query output to the unified shape.
    NOTE: confirm these key names with Person B.
    Expected input shape (example):
        {"id": 12, "row_summary": "...", "table": "experiments"}
    """
def normalize_web_result(r: dict) -> dict:
    """Adapt your own web_search_tool.py output to the unified shape."""
def merge_evidence(paper_results=None, db_results=None, web_results=None):
    """
    Combine all three sources into one flat, unified evidence pool.

    Args:
        paper_results: raw list from Person B's Papers tool (or None)
        db_results: raw list from Person B's Database tool (or None)
        web_results: raw list from web_search_tool.web_search() (or None)

    Returns:
        List[dict] in the unified evidence shape (see module docstring)
    """

"""
fact_checker.py
---------------
PART 3 of Person C's work: Fact-Checking.

Takes a claim (usually one line from the draft report, or the user's
original question) plus the merged evidence pool, and asks a local
LLM (via Ollama, same tool Person A uses for the planner) to judge
how well-supported that claim is across sources.

This is the "specialized, not generic" part you can defend in viva:
a generic chatbot answers from parametric memory with no way to say
"this is only backed by one weak source." This does that explicitly.
"""